#include "divide_event.h"

void divide_event_edge(XMLDataSet *dataset, char *pdata, int64_t length)
{
	//add length to pdata
}

int64_t divide_event(XMLDataSet *dataset, char *pdata)
{
	//return dataset length and add that to pdata
}
